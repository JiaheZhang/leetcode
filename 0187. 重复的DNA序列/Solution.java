class Solution {
	private static final LEN = 10;
	private List<String> res = new ArrayList<>();
	private Set<String> set = new HashSet<>();
    public List<String> findRepeatedDnaSequences(String s) {
    	if (s.length() < 2 * LEN) {
    		return res;
    	}
    	for (; ; ) {
    		
    	}
    }
}